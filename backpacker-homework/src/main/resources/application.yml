server:
 port: 8080
spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/backpacker
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show_sql: true
    hibernate:
     ddl-auto: none
logging:
 level:
  org:
   hibernate:
    type:
     descriptor:
      sql: trace